# Prevent npm from removing important files
save-prefix=~
package-lock=true
legacy-peer-deps=true

# Ensure Prisma and critical files are preserved
## Note: npm prune --production respects module dependencies
## All files under node_modules are preserved if the module is in package.json dependencies
